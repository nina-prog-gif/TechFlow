name: TechFlow QA Pipeline

'on':
  push:
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Exibir mensagem
        run: echo "Pipeline funcionando"